# 01. Create a virtual machine with docker-machine using the virtualbox driver,
# and named Char.

# Resources:
# https://docs.docker.com/machine/get-started/

docker-machine create --driver virtualbox Char
